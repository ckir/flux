\* recovery scenario, POSIX, strong identity, strong lock capability: the Recoverer/PlainRun pairing
\* of the check suite (design Section 12). All four actors at once do not exhaust in the budget, so
\* the scenario's check runs pair them instead; this one is where a plain rerun (21.1) meets a dead
\* owner's lock a Recoverer is working on. Measured: 1,620,690 distinct states, exhausts.
SPECIFICATION Spec
CONSTANTS
    Owners = {o1}
    Recoverers = {r1}
    PlainRuns = {p1}
    Cleanups = {}
    NoProc = nobody
    P = dir
    LockName = lock
    DirLockName = dirlock
    MaxObjs = 4
    MaxCrashes = 2
    HostCrashes = FALSE
    Platform = "posix"
    IdentityStrength = "strong"
    LockCapability = "strong"
    defaultInitValue = defaultInitValue
INVARIANTS
    FsOk
    Classifiable
    SingleWriter
    PlainNeverOwnsUncertain
    ForeignUntouched
    RefusalJustified
