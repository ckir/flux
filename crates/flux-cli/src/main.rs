//! Flux CLI (spec §3.6): argument parsing and command dispatch.
//!
//! Spec §4 defines the initial commands: `copy`, `verify`, `benchmark`,
//! `cleanup`. None are wired up yet — this is scaffold only.

fn main() {
    eprintln!("flux: not implemented yet");
    std::process::exit(1);
}
