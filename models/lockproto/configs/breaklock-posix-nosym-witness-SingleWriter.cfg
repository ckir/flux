\* breaklock scenario, POSIX, strong capability: MEASUREMENT run (plan 3, owner ruling 2026-09-14). Owner and two
\* Breakers as in breaklock-posix-check.cfg, without symmetry, halting at the first violation: does the 96.1
\* acquirer window, already seen on RefusalJustified, also let two writers pass their Section 99 checks?
SPECIFICATION Spec
CONSTANTS
    Owners = {o1}
    Recoverers = {}
    PlainRuns = {}
    Cleanups = {}
    Breakers = {b1, b2}
    NoProc = nobody
    P = dir
    LockName = lock
    DirLockName = dirlock
    MaxObjs = 3
    MaxCrashes = 2
    HostCrashes = FALSE
    MaxLeaseExpiries = 0
    Platform = "posix"
    IdentityStrength = "strong"
    LockCapability = "strong"
    SEED_RECOVER_FOREIGN = FALSE
    SEED_RECOVER_UNCERTAIN = FALSE
    SEED_DEAD_AS_BUSY = FALSE
    SEED_RECOVER_UNCERTAIN_CLEANUP_LOCK = FALSE
    defaultInitValue = defaultInitValue
INVARIANT
    SingleWriter
