# AGY-TEST-AUDIT ledger

One row per completed test audit: after a capstone, a peer audits whether the committed tests would catch the next
regression; every claimed gap is verified by measurement (a mutant that leaves every test green), and the owner scopes
what gets closed. Conventions: the newest row goes at the BOTTOM, and every `|` inside a cell is escaped as `\|`.

| Date | Audited range | Rounds | Verdict | Evidence | Discarded or deferred (anti-sweep) |
|---|---|---|---|---|---|
| 2026-09-14 | `bd7508a..1cfacc4` (audited `bd7508a..21bef41`, with the fixes through `1cfacc4`; lock-model plan 2; suites `models/lockproto/test_run.py`, `tests/model_stamp.rs`, `models/lockproto/test_workflow.py`, and the model's own invariants, witnesses and seeds) | 1 audit round (agy unreachable on quota; the owner replaced it with four read-only subagents), then capstone rounds 3 to 5 over the fixes | GAPS FOUND; every gap dispositioned | Brief `.clavity/seams/lock-model-plan2-test-audit-r1.md`. 25 unit-test gaps, each a mutant that left the suite green (control mutants were killed, so the harness was sound): FOLDED in `1b7426c`, and 4 more in `412512c`, each proven by its mutant now failing its new test. Model net: the four recovery safety invariants could not be told from `TRUE` (`ForeignUntouched` and `PlainNeverOwnsUncertain` vacuous by construction; `RefusalJustified` and `SingleWriter` unseeded): FOLDED, owner scope "Full fix now", in `1568175`, `a024c79`, `ed4dfd4` (four seeds, a `Foreign` start state, a state-predicate `ForeignUntouched`, the `NeverChecked` witness); CI Model `34868679580` green on `1cfacc4`. | DEFERRED-TO-ANOMALIES (2026-09-14): M5 `NeverTornRead` placement-blind (`algorithm.txt:89`), M6 `recoveredAfterCrash` guard always true (`algorithm.txt:263`), M7 host-crash net holes (`FsModel.tla:270`), M8 liveness consequent weakening (`invariants.txt:75`). DISCARDED-BELOW-FLOOR: `model_stamp.rs` duplicate and zero ordinals (caught transitively by the hash and quote checks); `run.py` open-finding-in-`violated` check (unreachable: `open_findings` is only allowed where `violated` is empty); the YAML side of `--list-jobs` (a drift fails CI visibly). |
| 2026-09-21 | `7ee6bd0..9fe19a7` (lock-model plan 3: the `breaklock`, `mixed`, `breaklock-remote` and `mixed-remote` scenarios; branch `plan3/closing`). Round 1 audited `models/lockproto/test_run.py`, `tests/model_stamp.rs`, `models/lockproto/test_workflow.py` and `models/lockproto/expected.toml`; round 2 was scoped to `expected.toml` ALONE at `88177d2`. Fixes through `a1be26c`. | 2, both on the live agy peer. **PRECONDITION DEVIATION, stated in both briefs:** the discipline says to run after the capstone is GREEN, and this one closed as a ROUND-CAP WAIVER at `fe36e2f` - so these rounds audited the safety net of code six capstone rounds never fully settled. | GAPS FOUND in both rounds. Round 1: four gaps, **all four real** - I ran every mutant, three survived and the fourth was structural. Round 2: four verified gaps and one re-raise rejected. The owner scoped round 2 to TB-1 and logged the rest as debt. | **Round 1, all folded.** TA-2 (`5fbeca7`): `test_every_config_declares_the_same_constants` asserted the gaps BETWEEN configs were empty and never that anything PARSED; mutant - `constant_names` returns nothing - left it green. Same shape as the `parse_cost_nodes` defect capstone round 6 found, and I wrote both. TA-3 (`5fbeca7`): no test for an unknown TLC exit status, so the catch-all default could be flipped to ACCEPT 77, 1 and 255. TA-4 (`5fbeca7`): `validate()` checked a WITNESS run's `.cfg` for its named invariant and never a SEEDED run's; mutant - point `seed.cfg` at `INVARIANT WrongName` - left it green. TA-1 (`88177d2`): `FsOk` and `Classifiable` had NO seed, so neither could be told from `TRUE`; two seeds corrupt `fs` from the ENVIRONMENT rather than through `FsModel`, keeping the module boundary. **CI Model `35579057283` (`88177d2`) green: `SEED_FS_LOCK_WITHOUT_HANDLE` expected=FsOk observed=FsOk states=4, `SEED_FS_ALIEN_CONTENT` expected=Classifiable observed=Classifiable states=14** - both seeds fire, so TA-1 is closed. **Round 2** audited the 74 runs of `expected.toml` as what they are - the protocol's own tests. Its CENSUS (every invariant x configs checking x runs seeding) was computed by me rather than asked of the peer, because round 1's coverage of that file was not credible: its `[VERIFIED:]` named five files without `expected.toml` and it quoted the second-to-last line. After TA-1 every invariant has a seed, `AtMostThree` the lone zero by design (the selftest's `open_findings`, `expected.toml:21-23`). **TB-1, the gap the owner scoped** (`a1be26c`): `RefusalJustified` is checked by 43 configs and was seeded by ONE, which exercises the `RefusalEvidence` family; the `lostLock` half - `S99_check`, `S99_release_check`, `S21_1_s3` - was seeded by nothing, and the mutant `refusedOk[self] := TRUE` at `S99_check` leaves the suite green. Those three labels are the ONLY readers of `StillOwned`, so one seed inside that operator guards the half: `SEED_CHECK_REFUSES_UNTOUCHED` fails every Section 99 check, and a holder nobody dispossessed refuses TARGET_LOCK_BUSY with its ghost still FALSE. **CI Model `35589123770` (`a1be26c`) green: `recovery-posix-seeded-SEED_CHECK_REFUSES_UNTOUCHED` expected=RefusalJustified observed=RefusalJustified states=6,105** - the seed reaches the refusal, so TB-1 is closed too. Briefs `.clavity/seams/plan3-test-audit-r1.md` and `-r2.md`; anomalies `.clavity/local-anomalies.md`. Gate at HEAD: 208 runner tests, `model_stamp` 45 passed 1 ignored, `--check-translation` clean, pcal.trans + SANY clean, typos clean. | **TB-2 REJECTED - a re-raise MY OWN BRIEF INVITED.** `ForeignUntouched`'s content half was dispositioned UNREACHABLE by the plan-2 audit; the discipline says to inline the accepted-boundary ledger EVERY round, and I inlined M5-M8 and the below-floor items but omitted the `Unreachable:` line. **DEFERRED to `.clavity/local-anomalies.md`, each with its mutant recorded so the next audit starts from them:** TB-3 - `FsOk` is three conjuncts and TA-1's seed breaks one, so `FsInvariants == LockImpliesHandle` stays green; TB-4 - `SEED_FS_ALIEN_CONTENT` writes `NoProc` specifically, so `Classifiable == \A o : fs.content[o] # NoProc` stays green. Those two are the same law a sixth consecutive time: **the fix undershoots** - the seed pins the seed, not the property, and 4 and 14 states say why (the environment corrupts `fs` instantly, nowhere near a protocol path). TB-5 - `touchedUncertain` is assigned at `algorithm.txt:299` and NOWHERE else, so only the move-aside is instrumented for `PlainNeverOwnsUncertain`. **TB-1's own residue, deferred:** a seeded run halts at the FIRST violating state, so the new run pins `S99_check` alone and a mutant hardwiring `refusedOk` at the other two labels still survives - the same per-site structure that needed five `NeverRefusedUnsafe` witnesses in capstone round 6. **Process:** round 1's peer wrote `models/lockproto/findings.md` into the repository and it was deleted; round 2's brief forbade writing any file at all. |
| 2026-09-23 | `a35fb8b..f2cc097` (walker PR 1, object identity; range re-extended over the fold). Suites audited: `crates/flux-platform/tests/std_fs.rs`, the `mod tests` in `crates/flux-fs/src/fs.rs`, and the `mod tests` in `crates/flux-core/src/fault_fs.rs`, together with the three source files they guard. Fixes at `f2cc097`. | 1 round, on the live agy peer. **PRECONDITION DEVIATION, stated in the brief:** the discipline runs after the capstone gate passes, and this one passed NEITHER way - no owner GREEN, no round-cap waiver, no `agy-capstone.head` marker, and the capstone ledger row records the verdict as UNADJUDICATED. The brief said so in one line so the peer would not read \"the capstone passed\" as \"the behaviour is correct\". | GAPS FOUND. Three claimed, **all three verified by measurement**, two FOLDED and one DISCARDED-BELOW-FLOOR. | **FOLDED (`f2cc097`), PA-1:** `is_file == false` for reparse points was tested only under `#[cfg(unix)]`, and that gate's own comment justified itself by symlinks needing Developer Mode on Windows - a justification THIS PR had already made obsolete by using a junction, which needs none. Windows is precisely where `metadata` and `symlink_metadata` diverge on reparse points. New test `metadata_reports_a_reparse_point_as_not_a_file`; non-vacuous, forcing `is_file: true` turns it red (11 passed 2 failed). **FOLDED (`f2cc097`), from the peer's PROSE rather than its structured findings:** `move_object` carried `files`, `times` and `perms` but not `identities`, so a rename minted a NEW object - the path-derived default changed with the path, and even a `set_identity` value stayed keyed to the old name. A real filesystem preserves the inode, so section 149.4's question (did the object under this path change?) was unanswerable through the fake. New test `an_identity_follows_the_object_through_a_rename`; non-vacuous, dropping the carry turns it red (31 passed 1 failed). Counts after: Windows `tests/std_fs.rs` 13, `flux-core` 32, `flux-fs` 10; Linux `tests/std_fs.rs` 15, `flux-core` 32, `flux-fs` 10; `just check` exit 0. Brief `.clavity/seams/pr1-test-audit.md`. | **DISCARDED-BELOW-FLOOR, MD-1 (the peer rated it blocking; measurement stood it down):** all three Windows `identity_of` failure arms are uncovered - MEASURED, `panic!()` in the open-failure arm leaves the suite at 12 passed 0 failed. The structural guard that makes them unreachable: `identity_of` opens with `.access_mode(0)` plus `FILE_FLAG_BACKUP_SEMANTICS | FILE_FLAG_OPEN_REPARSE_POINT`, the SAME flags std's `lstat` uses (`library/std/src/sys/fs/windows.rs:1484-1485, 1501-1504`), so the `symlink_metadata` call earlier in `metadata()` performs the same open and fails FIRST. MEASURED directly: holding the file at `share_mode(0)` fails neither open - `access_mode(0)` requests no access rights, so Windows grants it regardless of sharing - and identity came back `Strong`. The arms are therefore reachable only under the TOCTOU race the capstone already recorded, which no deterministic integration test can produce. **DEFERRED-TO-ANOMALIES, AS-1 * 2026-09-23:** the reparse-point helper is a dual-path fallback, so a machine where `symlink_dir` succeeds (CI runners are admin) never executes the `mklink` junction path; a broken fallback would stay green there. Bounded rather than silent - it fails LOUDLY on an unprivileged machine, which is how the original defect was found. Peer's own below-floor list: the FNV prime mutation (mutating it to 1 leaves the fake's test green - CONFIRMED by measurement, 31 passed; I agree the hash itself is an implementation detail, and the real issue it pointed at is the rename defect folded above), and the Unix `inode == 0` guard (agreed unreachable: no Unix kernel issues inode 0 to a valid file). |
| 2026-09-24 | `f2cc097..80b2644` (walker PR 1, RE-AUDIT of the identity-model rewrite. The first audit stopped at `f2cc097` and 185 lines of `crates/flux-core/src/fault_fs.rs` changed after it - `mint_identity`, `missing_source`, the replace semantics in `move_object`, the clearing in `remove_file`, the `metadata` panic, and six tests - so the safety-net question had never been asked of that code. Range re-extended over the fold at `80b2644`.) | 1 round, live agy peer | GAPS FOUND. Two claimed: one FOLDED, one REJECTED by measurement. | **PRECONDITION, stated in the brief:** the capstone closed as a **ROUND-CAP WAIVER** at `c916a95`, not a GREEN - both `.clavity/agy-marks/skipped.log` and the capstone ledger agree, so the peer was told the code under audit shipped with `c916a95` unreviewed and must not read \"the capstone passed\" as \"the behaviour is correct\". **FOLDED, STC-1 (`80b2644`):** `mint_identity`'s early return - the guard that stops a truncating write re-minting an identity - was never exercised. MEASURED: deleting it left the whole suite GREEN at 36 passed. I had guessed this same gap independently before the reply arrived and confirmed it with the same mutant, so driver and peer converged on it separately. New test `writing_over_an_existing_path_preserves_its_identity`; non-vacuous against the IDENTICAL mutant that previously left the suite green - 36 passed 1 failed, and the failure is that test. Object continuity across a write is what a walk comparing a directory against its ancestors will rest on. Counts after: Windows and Linux both `flux-core` 37, `flux-fs` 10, `tests/std_fs.rs` 13 Windows / 15 Linux; `just check` exit 0. Brief `.clavity/seams/pr1-test-audit-r2.md`. | **REJECTED by measurement, STC-2:** the audit called `rename_no_replace` uncovered and predicted that a regression in its target-exists check \"would silently break the engine's atomic publish guarantees\". True of this file's own test module, FALSE of the system - MEASURED, replacing that check with `if false` turns `copy::tests::no_replace_refuses_an_existing_target_and_cleans_up` RED (35 passed, 1 failed). A coverage question scoped to one module yielded a system-wide conclusion that does not hold, which is exactly why every claimed gap is re-run before folding. **Peer's below-floor list, with my note on the second:** (1) `rename_no_replace` against a MISSING destination, which delegates to `move_object` and is covered through `rename_replace` - agreed; (2) `remove_file` against a missing path, which the peer stood down because `it_records_the_order_of_calls` \"happens to execute this exact path and ignores the `Err`\". Executing a branch while ignoring its result is NOT coverage, and the peer conflated the two - the stand-down is accepted on reachability grounds rather than the reason given. **Still open from audit round 1, not re-raised:** the reparse-point helper's dual-path masking (`DEFERRED-TO-ANOMALIES * 2026-09-23`), and the three Windows `identity_of` failure arms (`DISCARDED-BELOW-FLOOR` on the cited same-flags-as-lstat guard). |
| 2026-09-24 | `ec58829..50ad445` (audited `ec58829..5263ca6`, with the fixes through `50ad445`; walker PR 2, the directory walk). Suites audited: the `mod tests` in `crates/flux-core/src/walk.rs` and `crates/flux-core/src/fault_fs.rs`, and `crates/flux-platform/tests/std_fs.rs`, together with the four source files they guard. | 1 round, live agy peer | **PRECONDITION: a CLEAN GREEN, not a waiver** - `.clavity/agy-marks/skipped.log` has no `WAIVED` line for any sha in this PR and the capstone ledger positively records "GREEN, owner-confirmed at `5263ca6`", so the peer was briefed as normal. GAPS FOUND. Four claimed by the peer, a fifth found independently by the driver; **all five verified by a mutant that SURVIVED the suite** (each left `64 passed`), and the owner scoped "close all five". | **FOLDED, all five, at `50ad445`; each new test proven non-vacuous against the IDENTICAL mutant that had left the suite green, and in every case the SPECIFIC new test is the one that went red.** (1) mid-walk `metadata` failure: no test injected a metadata fault ANYWHERE, so `enter`'s error arm was unguarded - the only metadata failures tested were on the root, which fails before `enter` is reached. Test `a_metadata_failure_mid_walk_is_reported_and_the_walk_continues`. (2) type-change to a FILE: the guard was exercised only by a swap to a Symlink, so weakening `!= FileType::Dir` to `== FileType::Symlink` AT THE ENTER SITE survived. Test `an_entry_listed_as_a_dir_that_became_a_file_is_refused`. (3) cycle detection AFTER an error: the existing balance test proved an error causes no false POSITIVE later and nothing proved it causes no false NEGATIVE, so replacing the unwind's `pop()` with `clear()` survived - and `clear()` discards the ROOT's id, silently disabling cycle detection for the rest of the walk. Test `an_error_does_not_disable_cycle_detection_for_a_later_sibling`, shaped on the measured `mount --bind a a/b` case where the re-entered directory IS the root. (4) root `read_dir` failure: both root refusals tested fail at `metadata`, so that path was never reached and `unwrap_or_default()` survived. Test `walk_refuses_a_root_whose_read_dir_fails`. (5) **`WalkEvent::Other` was never yielded by ANY test** - an entire event variant with zero coverage - so rewriting that arm to produce `File` survived. Test `a_special_file_is_reported_as_other_and_never_descended_into`. Test-only: no source changed, so the capstone GREEN still describes the code and no re-capstone was owed. Counts after: Windows `158 tests run: 158 passed, 1 skipped`, Linux 160 passed. Brief `.clavity/seams/pr2-test-audit.md`. | **THE PEER MISSED GAP (5), WHICH THE DRIVER FOUND INDEPENDENTLY** - the discipline's own stated false-negative limitation, observed rather than assumed: a single-peer audit is a floor, not proof. **METHOD CAUTION worth carrying forward:** the first mutant for gap (2) replaced BOTH `if m.file_type != FileType::Dir` sites and killed `walk_refuses_a_root_that_is_not_a_directory`, the ROOT check - had the suite's non-zero exit alone been credited, a REAL gap would have been falsely REJECTED. Confirming WHICH test went red is what caught it, and the re-run against the enter site alone left `64 passed`. **Peer's below-floor list, both agreed:** `max_depth = 0` has no test, so `&& self.max_depth != 0` would survive - debt, since 256 is the default and a boundary bug at 0 is theoretical; and `StdFileSystem::create_dir` over a FILE is untested while `FaultFs` covers it - debt, since the OS itself guarantees that refusal. **Peer schema breach, substance unaffected:** its json used `claim-type` values `missing-coverage`, `weak-assertion` and `state-corruption`, none of which are in this discipline's declared set. |
| 2026-09-25 | `209dc19..3f8c79c` (audited the 149.7 handle-relative destination writes suites at `209dc19`, with the fixes through `3f8c79c`; branch `feat/handle-relative-writes`). Suites audited: `crates/flux-platform/tests/dir_handle.rs`, the `mod tests` in `crates/flux-fs/src/name.rs`, and the `mod tests` in `crates/flux-core/src/fault_fs.rs`, together with the six source files they guard. | 1 audit round on the live agy peer, then 2 capstone rounds over the fixes (required by the capstone-invalidation rule, because closing one gap changed executable code) | **PRECONDITION: a CLEAN GREEN, not a waiver.** The capstone ledger records GREEN at round 12, owner-confirmed at `209dc19`, and the owner had waived the round-6 cap only to keep the review GOING -- the opposite of a completion waiver -- so the peer was briefed as normal. GAPS FOUND: three claimed, **all three verified by a mutant that left the suite GREEN**, and the owner scoped "fold it" on the one that needed a source change. | **FOLDED, G1 and G3 at `e719548`.** G1: the code-unit validation path enforces four rules and its test supplied three -- MEASURED, deleting the NUL arm left the whole suite green; the corpus is now every rule that path enforces. G3: `rename_at` passes FILE_OPEN_REPARSE_POINT so a rename acts on the LINK, and MEASURED, removing that flag left all 207 tests green -- `remove_file` and `create_new` each had a guard and rename had none. New test asserts the junction moved, its target did not, the target's contents are untouched, and the moved name still resolves through. **FOLDED, G2, the one that needed source: `4c4b8c5` + `08dede4`.** Both layered reparse guards refuse when a reparse point is present but its tag cannot be read -- the branch that stops an unjudgeable surrogate being traversed -- and flipping EITHER to fail-open left the entire suite green, because the precondition is unreachable on every filesystem available (NTFS always answers the tag query; FAT32 has no reparse points). The decision is now separated from the syscalls: an `AttrQuery` says what the queries answered and two pure judges decide from that alone, so every branch is reachable from a unit test. **The two mutants that previously went undetected now red exactly the two new tests and nothing else.** Behaviour unchanged (the same 208 tests pass either side; the FAT32 pair still passes against a real volume). Capstone re-run round 1 then found two more in the new seam, both folded at `08dede4`: `Tagged` was built without checking the reparse bit, and `Untagged` with the directory attribute -- the exact FAT32 case the fallback exists for -- was the one row of the table with no unit test. Round 2 returned GREEN with an empty array. Brief `.clavity/seams/item-b-test-audit.md`. Gates at HEAD: Windows `just check` 216 passed / 2 skipped; WSL workspace 196 passed / 2 skipped; the two FAT32 tests pass against a real volume at `Y:`. | **THE DRIVER FOUND G2 INDEPENDENTLY AND FIRST** -- measured by mutation before the peer's reply arrived, and the peer then agreed it was the weakest point; the brief had named it as the driver's own guess, so this is convergence rather than discovery. **MECHANISM REJECTED while the finding was FOLDED (capstone re-run round 1):** the peer argued the tag field could hold uninitialised kernel pool memory that might match the surrogate mask and delete a plain directory. MEASURED FALSE -- both wrappers zero-initialise the struct at `dir_windows.rs:787` and `:892`, so an unwritten tag reads 0 and `is_name_surrogate(0)` is false, which refuses. The STRUCTURAL half earned the fold on its own: consulting a field the NT ABI says to ignore is relying on unspecified behaviour whatever the current kernel writes. **DISCARDED-BELOW-FLOOR (the peer's own list):** `create_dir_refuses_a_missing_parent_and_an_occupied_name` in `FaultFs` asserts only `is_err()` rather than the exact kind -- pre-existing, and the trait-parity test added in capstone round 10 now pins those kinds at the trait level, which is the compensation. **Process:** the peer respected the review-only envelope this time, after breaching it in at least five capstone rounds; the difference was being pointed at a seam file rather than asked to investigate. |
