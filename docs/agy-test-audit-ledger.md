# AGY-TEST-AUDIT ledger

One row per completed test audit: after a capstone, a peer audits whether the committed tests would catch the next
regression; every claimed gap is verified by measurement (a mutant that leaves every test green), and the owner scopes
what gets closed. Conventions: the newest row goes at the BOTTOM, and every `|` inside a cell is escaped as `\|`.

| Date | Audited range | Rounds | Verdict | Evidence | Discarded or deferred (anti-sweep) |
|---|---|---|---|---|---|
| 2026-09-14 | `bd7508a..1cfacc4` (audited `bd7508a..21bef41`, with the fixes through `1cfacc4`; lock-model plan 2; suites `models/lockproto/test_run.py`, `tests/model_stamp.rs`, `models/lockproto/test_workflow.py`, and the model's own invariants, witnesses and seeds) | 1 audit round (agy unreachable on quota; the owner replaced it with four read-only subagents), then capstone rounds 3 to 5 over the fixes | GAPS FOUND; every gap dispositioned | Brief `.clavity/seams/lock-model-plan2-test-audit-r1.md`. 25 unit-test gaps, each a mutant that left the suite green (control mutants were killed, so the harness was sound): FOLDED in `1b7426c`, and 4 more in `412512c`, each proven by its mutant now failing its new test. Model net: the four recovery safety invariants could not be told from `TRUE` (`ForeignUntouched` and `PlainNeverOwnsUncertain` vacuous by construction; `RefusalJustified` and `SingleWriter` unseeded): FOLDED, owner scope "Full fix now", in `1568175`, `a024c79`, `ed4dfd4` (four seeds, a `Foreign` start state, a state-predicate `ForeignUntouched`, the `NeverChecked` witness); CI Model `34868679580` green on `1cfacc4`. | DEFERRED-TO-ANOMALIES (2026-09-14): M5 `NeverTornRead` placement-blind (`algorithm.txt:89`), M6 `recoveredAfterCrash` guard always true (`algorithm.txt:263`), M7 host-crash net holes (`FsModel.tla:270`), M8 liveness consequent weakening (`invariants.txt:75`). DISCARDED-BELOW-FLOOR: `model_stamp.rs` duplicate and zero ordinals (caught transitively by the hash and quote checks); `run.py` open-finding-in-`violated` check (unreachable: `open_findings` is only allowed where `violated` is empty); the YAML side of `--list-jobs` (a drift fails CI visibly). |
| 2026-09-21 | `7ee6bd0..9fe19a7` (lock-model plan 3: the `breaklock`, `mixed`, `breaklock-remote` and `mixed-remote` scenarios; branch `plan3/closing`). Round 1 audited `models/lockproto/test_run.py`, `tests/model_stamp.rs`, `models/lockproto/test_workflow.py` and `models/lockproto/expected.toml`; round 2 was scoped to `expected.toml` ALONE at `88177d2`. Fixes through `a1be26c`. | 2, both on the live agy peer. **PRECONDITION DEVIATION, stated in both briefs:** the discipline says to run after the capstone is GREEN, and this one closed as a ROUND-CAP WAIVER at `fe36e2f` - so these rounds audited the safety net of code six capstone rounds never fully settled. | GAPS FOUND in both rounds. Round 1: four gaps, **all four real** - I ran every mutant, three survived and the fourth was structural. Round 2: four verified gaps and one re-raise rejected. The owner scoped round 2 to TB-1 and logged the rest as debt. | **Round 1, all folded.** TA-2 (`5fbeca7`): `test_every_config_declares_the_same_constants` asserted the gaps BETWEEN configs were empty and never that anything PARSED; mutant - `constant_names` returns nothing - left it green. Same shape as the `parse_cost_nodes` defect capstone round 6 found, and I wrote both. TA-3 (`5fbeca7`): no test for an unknown TLC exit status, so the catch-all default could be flipped to ACCEPT 77, 1 and 255. TA-4 (`5fbeca7`): `validate()` checked a WITNESS run's `.cfg` for its named invariant and never a SEEDED run's; mutant - point `seed.cfg` at `INVARIANT WrongName` - left it green. TA-1 (`88177d2`): `FsOk` and `Classifiable` had NO seed, so neither could be told from `TRUE`; two seeds corrupt `fs` from the ENVIRONMENT rather than through `FsModel`, keeping the module boundary. **CI Model `35579057283` (`88177d2`) green: `SEED_FS_LOCK_WITHOUT_HANDLE` expected=FsOk observed=FsOk states=4, `SEED_FS_ALIEN_CONTENT` expected=Classifiable observed=Classifiable states=14** - both seeds fire, so TA-1 is closed. **Round 2** audited the 74 runs of `expected.toml` as what they are - the protocol's own tests. Its CENSUS (every invariant x configs checking x runs seeding) was computed by me rather than asked of the peer, because round 1's coverage of that file was not credible: its `[VERIFIED:]` named five files without `expected.toml` and it quoted the second-to-last line. After TA-1 every invariant has a seed, `AtMostThree` the lone zero by design (the selftest's `open_findings`, `expected.toml:21-23`). **TB-1, the gap the owner scoped** (`a1be26c`): `RefusalJustified` is checked by 43 configs and was seeded by ONE, which exercises the `RefusalEvidence` family; the `lostLock` half - `S99_check`, `S99_release_check`, `S21_1_s3` - was seeded by nothing, and the mutant `refusedOk[self] := TRUE` at `S99_check` leaves the suite green. Those three labels are the ONLY readers of `StillOwned`, so one seed inside that operator guards the half: `SEED_CHECK_REFUSES_UNTOUCHED` fails every Section 99 check, and a holder nobody dispossessed refuses TARGET_LOCK_BUSY with its ghost still FALSE. **CI Model `35589123770` (`a1be26c`) green: `recovery-posix-seeded-SEED_CHECK_REFUSES_UNTOUCHED` expected=RefusalJustified observed=RefusalJustified states=6,105** - the seed reaches the refusal, so TB-1 is closed too. Briefs `.clavity/seams/plan3-test-audit-r1.md` and `-r2.md`; anomalies `.clavity/local-anomalies.md`. Gate at HEAD: 208 runner tests, `model_stamp` 45 passed 1 ignored, `--check-translation` clean, pcal.trans + SANY clean, typos clean. | **TB-2 REJECTED - a re-raise MY OWN BRIEF INVITED.** `ForeignUntouched`'s content half was dispositioned UNREACHABLE by the plan-2 audit; the discipline says to inline the accepted-boundary ledger EVERY round, and I inlined M5-M8 and the below-floor items but omitted the `Unreachable:` line. **DEFERRED to `.clavity/local-anomalies.md`, each with its mutant recorded so the next audit starts from them:** TB-3 - `FsOk` is three conjuncts and TA-1's seed breaks one, so `FsInvariants == LockImpliesHandle` stays green; TB-4 - `SEED_FS_ALIEN_CONTENT` writes `NoProc` specifically, so `Classifiable == \A o : fs.content[o] # NoProc` stays green. Those two are the same law a sixth consecutive time: **the fix undershoots** - the seed pins the seed, not the property, and 4 and 14 states say why (the environment corrupts `fs` instantly, nowhere near a protocol path). TB-5 - `touchedUncertain` is assigned at `algorithm.txt:299` and NOWHERE else, so only the move-aside is instrumented for `PlainNeverOwnsUncertain`. **TB-1's own residue, deferred:** a seeded run halts at the FIRST violating state, so the new run pins `S99_check` alone and a mutant hardwiring `refusedOk` at the other two labels still survives - the same per-site structure that needed five `NeverRefusedUnsafe` witnesses in capstone round 6. **Process:** round 1's peer wrote `models/lockproto/findings.md` into the repository and it was deleted; round 2's brief forbade writing any file at all. |
| 2026-09-23 | `a35fb8b..f2cc097` (walker PR 1, object identity; range re-extended over the fold). Suites audited: `crates/flux-platform/tests/std_fs.rs`, the `mod tests` in `crates/flux-fs/src/fs.rs`, and the `mod tests` in `crates/flux-core/src/fault_fs.rs`, together with the three source files they guard. Fixes at `f2cc097`. | 1 round, on the live agy peer. **PRECONDITION DEVIATION, stated in the brief:** the discipline runs after the capstone gate passes, and this one passed NEITHER way - no owner GREEN, no round-cap waiver, no `agy-capstone.head` marker, and the capstone ledger row records the verdict as UNADJUDICATED. The brief said so in one line so the peer would not read \"the capstone passed\" as \"the behaviour is correct\". | GAPS FOUND. Three claimed, **all three verified by measurement**, two FOLDED and one DISCARDED-BELOW-FLOOR. | **FOLDED (`f2cc097`), PA-1:** `is_file == false` for reparse points was tested only under `#[cfg(unix)]`, and that gate's own comment justified itself by symlinks needing Developer Mode on Windows - a justification THIS PR had already made obsolete by using a junction, which needs none. Windows is precisely where `metadata` and `symlink_metadata` diverge on reparse points. New test `metadata_reports_a_reparse_point_as_not_a_file`; non-vacuous, forcing `is_file: true` turns it red (11 passed 2 failed). **FOLDED (`f2cc097`), from the peer's PROSE rather than its structured findings:** `move_object` carried `files`, `times` and `perms` but not `identities`, so a rename minted a NEW object - the path-derived default changed with the path, and even a `set_identity` value stayed keyed to the old name. A real filesystem preserves the inode, so section 149.4's question (did the object under this path change?) was unanswerable through the fake. New test `an_identity_follows_the_object_through_a_rename`; non-vacuous, dropping the carry turns it red (31 passed 1 failed). Counts after: Windows `tests/std_fs.rs` 13, `flux-core` 32, `flux-fs` 10; Linux `tests/std_fs.rs` 15, `flux-core` 32, `flux-fs` 10; `just check` exit 0. Brief `.clavity/seams/pr1-test-audit.md`. | **DISCARDED-BELOW-FLOOR, MD-1 (the peer rated it blocking; measurement stood it down):** all three Windows `identity_of` failure arms are uncovered - MEASURED, `panic!()` in the open-failure arm leaves the suite at 12 passed 0 failed. The structural guard that makes them unreachable: `identity_of` opens with `.access_mode(0)` plus `FILE_FLAG_BACKUP_SEMANTICS | FILE_FLAG_OPEN_REPARSE_POINT`, the SAME flags std's `lstat` uses (`library/std/src/sys/fs/windows.rs:1484-1485, 1501-1504`), so the `symlink_metadata` call earlier in `metadata()` performs the same open and fails FIRST. MEASURED directly: holding the file at `share_mode(0)` fails neither open - `access_mode(0)` requests no access rights, so Windows grants it regardless of sharing - and identity came back `Strong`. The arms are therefore reachable only under the TOCTOU race the capstone already recorded, which no deterministic integration test can produce. **DEFERRED-TO-ANOMALIES, AS-1 * 2026-09-23:** the reparse-point helper is a dual-path fallback, so a machine where `symlink_dir` succeeds (CI runners are admin) never executes the `mklink` junction path; a broken fallback would stay green there. Bounded rather than silent - it fails LOUDLY on an unprivileged machine, which is how the original defect was found. Peer's own below-floor list: the FNV prime mutation (mutating it to 1 leaves the fake's test green - CONFIRMED by measurement, 31 passed; I agree the hash itself is an implementation detail, and the real issue it pointed at is the rename defect folded above), and the Unix `inode == 0` guard (agreed unreachable: no Unix kernel issues inode 0 to a valid file). |
