# AGY-CAPSTONE ledger

One row per completed capstone review of a committed implementation: an independent peer (agy) tore down the code a
plan produced, every finding was verified by measurement before it was folded, and the owner adjudicated GREEN. The
evidence column names things a reader can check: fold commits, and the review transcript.

| Date | Range reviewed | Rounds | Verdict | Evidence | Discarded or deferred (anti-sweep) |
|---|---|---|---|---|---|
| 2026-09-11 | `ce31690..6c11719` (lock-model plan 1, tooling; branch `model/lock-protocol`) | 3 (2 + 1 owner-authorized verification round on `dc40569..6c11719`) | GREEN, owner-confirmed at `6c11719` | Folds: `2c65020` (a `check` run must list a reachability witness), `6c11719` (stamp closes fences by the CommonMark rule; README override `just python=python model`). Transcript: agy cascade `8976a01d-55fa-4532-9021-02ad9adf8cb5`. Gate at `6c11719`: `just check` 29 run / 29 passed / 1 skipped. | CA-1 rejected (numbered steps inside fences are units by design Section 9.1). FA-1 deferred: a backtick fence opener whose info string holds a backtick; latent, 0 instances in the spec; with plan 2's stamp work. Owner ruled two design gaps into plan 2: new sibling spec sections are invisible to the stamp; `liveness` runs have no reachability guard. Round 2's peer wrote a stray copy of a committed file; it was deleted, and the verification round ran with stricter no-write rules and wrote nothing. |
| 2026-09-14 | `bd7508a..b0ad304` (lock-model plan 2: FsModel, LockProtocol, the recovery scenario; branch `model/lock-protocol-wip`, PR #3) | 2 (owner cap) | ROUND-CAP WAIVER by the owner at `b0ad304`; the fold `3bed74f` was not reviewed by a further agy round | Folds: `9307b29` (a run's `.cfg` may not contain `CONSTRAINT`, `ACTION_CONSTRAINT` or `VIEW`; measured accepted before the fix), `3bed74f` (TLC 2.19's `TYPE` and `TYPE_CONSTRAINT` keywords refused too, read from the jar's `ModelConfig` keyword table). Briefs `.clavity/seams/lock-model-plan2-capstone.md` and `-r2.md`; transcript: agy cascade `b3c23210-1e89-402a-aed1-69f7a96602cf`. Gate: 177 runner unit tests OK; `cargo test --test model_stamp` 38 passed, 1 ignored; CI on the prior head green (Model, extended tier, union job, Rust CI). | Discarded below the floor: forged TLC message frames through `PrintT` (no model source calls `Print` or `PrintT`, and the model's author already controls every verdict). Rejected with evidence: `VIEWS` unmapped (not a TLC keyword); `touchedUncertain` vacuous (it is set at the only foreign-lock mutation, `algorithm.txt:207-209`; a regression guard); `tightened` not compared (design Section 12 says it annotates, never fails); torn read at `S240_3_s1` (its ghost feeds only a witness already violated); block comments parsed as labels (measured: not); union skipped per PR (by design, Section 4); change pattern omits `model-extended.yml` (no per-PR job reads it). |
| 2026-09-14 | `21bef41..1cfacc4` (lock-model plan 2, the test-audit fix: recovery seeds, the `Foreign` start state, `NeverChecked`, 27 new tests; branch `model/lock-protocol-wip`, PR #3) | 3 (rounds 3 to 5 of plan 2; peers were subagents because agy hit its quota and the owner ruled "Replace it with subagents") | GREEN at `1cfacc4`: round 5 had no findings | Folds: `412512c` (exit 11 and 13 agreement and the trace.toml parse message pinned; each proven by its mutant), `a024c79` (seed `SEED_RECOVER_UNCERTAIN_CLEANUP_LOCK`, the `ownerLive` half of the uncertain judgement), `ed4dfd4` (`SEED_RECOVER_UNCERTAIN` narrowed to a torn or empty record: it used `JudgedUncertain` itself, so an `ownerLive`-only mutant passed every run), design `8d394a7`, `8d81462`, `efd846a`. CI: Model `34861379897` (`3ba8c581ab8ff7c10c5d590da84e4a1e8ccbec2b`), extended tier and union `34861387714` (`3ba8c581ab8ff7c10c5d590da84e4a1e8ccbec2b`), Model `34868679580` and CI green on `1cfacc4`; every seeded run stopped with its named invariant. Gate: 196 runner unit tests OK; `cargo test --test model_stamp` 45 passed, 1 ignored. | Accepted: a change that bypasses `RefusalEvidence` at one label is not caught; seeds are POSIX-only; `SingleWriter`'s seeds belong to `mixed` and `breaklock`. Unreachable: `ForeignUntouched`'s content half (a `Foreign` object exists only initially, and every record write follows its writer's own exclusive create). Not modelled: a `Foreign` object appearing mid-run. Below the floor: deleting the new `/\ ownerLive[p] = "none"` conjunct changes no verdict today. Deferred to the anomalies inbox: M5 to M8 (witness placement, host-crash witness holes, liveness consequent). Correction to the plan-2 row above: its rejection of "`touchedUncertain` vacuous" was wrong; the test audit showed `Recover` runs only after `Replaceable`, so that ghost could never fire unseeded. |
| 2026-09-21 | `7ee6bd0..fe36e2f` (lock-model plan 3: the `breaklock`, `mixed`, `breaklock-remote` and `mixed-remote` scenarios, plus the CI topology they needed; branch `plan3/closing`) | 6 (owner cap) | **ROUND-CAP WAIVER by the owner at `fe36e2f`** - the review never reached GREEN. Every one of the six rounds produced a finding, and the last three blocking findings were in CLAIMS rather than behaviour. Round 6's folds (`cb5293f`, `fe36e2f`) were not reviewed by a further round. | **Blocking, one per round, each measured before folding:** MG-5 - `FIX_REMOTE_LEASE_SPEC` REPLACES `Superseded` rather than narrowing it, so a clean `-fixed-check` could not be told from a vacuous one (`945cd77`, the `NoLiveWriter` witness). DC-1 - `S240_5_s6`'s refusal evidence restated its own branch guard, so `RefusalJustified` could never fail there; measured at 3,569 violating states once the real predicate was asked, with a trace showing a Breaker reporting the lock held while the only Owner was CRASHED. That amended the SPEC: 240.5 step 6 now restarts instead of refusing (`788267a`), and the violations went to zero. SPEC-1 - the amendment left normative acceptance item 128 demanding the outcome it had just forbidden, where `spec-sections.stamp`'s twelve headings structurally cannot see it (`feea0e8`). DC2-1 - `or {}` turned `parse_coverage`'s deliberate fail-closed `None` into "this run covered nothing", which fails OPEN for the never_reached/deferred checks (`feea0e8`). R4-1 - the 235.1 capability refusal was documented as covered by the one run that seeds its BYPASS; fixed across three rounds and five one-actor witnesses (`3621a0c`, `b9bc50a`, `cb5293f`) because the first two fixes reached 1 of 5 and 4 of 5 sites. FA6-2 - `parse_cost_nodes` matched `^\s*line` while TLC prefixes nested nodes with `|`, so it read 10 of 17 and looked complete. CM-2 - the union job still skipped on a red build: GitHub implies `success()` over `needs` unless the `if` carries a status-check function (`fe36e2f`). **Infrastructure built by the review:** the suite-wide coverage union, which had been killed at its 180-minute cap every run and so had NEVER judged anything, rebuilt from the scenario jobs' own artifacts (`9bd099d`) - first verdict `103 labels covered, 0 never_reached, 1 deferred`; a `translation` job and a no-Java test, after `grep -rn pcal` over the justfile, every workflow and both test files returned nothing (`afc5c26`, `38474cd`); `breaklock-remote` split from 2h27m-2h36m against a 180-minute cap into two parallel jobs of 1h55m and 35m (`38474cd`). **CI:** Model `35224868930` (`788267a`) fully green including the first CI-judged union; `35559893031` (`3621a0c`) green; `35562847827` and `35568773588` in flight at `894e7b0` and `fe36e2f`. **Gate at HEAD:** 207 runner tests, `model_stamp` 45 passed 1 ignored, `--check-translation` clean, pcal.trans + SANY clean, typos clean. Briefs `.clavity/seams/plan3-capstone*.md`; anomalies `.clavity/local-anomalies.md`. | **THE FINDING THAT OUTLIVES THE REVIEW:** of ~84 factual claims in this branch's commit messages, ~45 are unverifiable from the repository FOREVER - every state count, every timing, all sixteen CI run ids; GitHub logs age out at 90 days and the messages do not. Of the ~39 checkable, 31 true and 8 false. Several uncheckable figures are the sole justification for an owner ruling that amended the spec. Cheapest remedy, not done: commit the union verdict as an artifact. **Deferred, tracked in `.clavity/local-anomalies.md`:** the label-granular coverage blind spot, which produced TWO findings - `--zero-branches` now reads TLC's expression-granular data but is a REPORT, not a gate, and no recorded fixture contains the blind spot's actual shape (a zero branch inside a COVERED action), so the instrumentation is unproven; no test exercises PlusCal cost-node shapes, which is what this suite emits; `IdentityStrength = "weak"` is dead across 74 of 74 runs while `FsModel.tla` implements it in four places; `fs.next <= Cardinality(Procs)` and "one filesystem operation per label" are both stated as guarantees that nothing enforces; a halting run's coverage is a PREFIX and the union now reports that rather than fixing it, which is unfixable from a halting run by construction; `pendingUnlink` could be a boolean (a real state-space reduction at 60M+ states); DC2-4, the `cargo-mutants` doc/JSON requiredness mismatch, is open on branch `chore/tooling-doc-guard`. **Rejected with evidence:** AB-1 (overturned in round 2 and folded); MG-2 (its mutant is caught by the sibling `-fixed-check`); MG-6 (the selftest's declared purpose); F1 (all four `FsTryLock` sites lock a freshly created or opened object); SC-2/3 (holders marked at the move-aside; `BrokenOf` is self-scoped); SC-4 (clearing `refusedOk` on crash would make `RefusalJustified` FALSE wherever a process refused then crashed); NL-1 (both commits regenerate byte-identically; `chksum(pcal)` is comment-SENSITIVE); and the shared-agy-channel theory in `SHAREDCHANELBUG.md`, refuted by measurement - three agy processes, one per workspace. **Below the floor:** AB-4, F2. **Process:** three review-only breaches (two scratch copies, a detached HEAD, eleven scratch files swept in by my own `git add -A` and stripped by amend); a negative `grep` with no positive control nearly produced a false finding. |
| 2026-09-22 | `fe28ec2..479b117` (flux-fs trait surface + single-file copy: the first product code in the repository - `flux-fs` error codes/options/traits, `FaultFs`, `copy_file`, `StdFileSystem`, CLI wiring, the §241.5 amendment and Task 10's limitations; branch `spec/flux-fs-copy`, unpushed) | 1 | **BREACH WAIVER by the owner at `479b117`** - the peer edited `Cargo.toml` and `Cargo.lock` to add `.clavity/scratch/fluxfs-copy-capstone/test_path` to the workspace `members` so it could compile and run a scratch crate, despite an explicit review-only banner. Both reverted (clean in the before-snapshot), HEAD never moved. Per the discipline a breach waiver writes the audit line and **NO completion marker**, so the gate re-arms; the fold `36a7919` was not reviewed by a further round. | **The one blocking finding, and the only one the six pre-execution panel rounds structurally could not reach:** publishing over an existing READ-ONLY destination diverged by platform. Measured on the same code path - Linux `std::fs::rename(temp, dst)` returns `Ok(())` and replaces it; Windows returns `PermissionDenied` and leaves it. Windows was the correct one: `rename` consults the DIRECTORY's write permission, not the file's, so on Unix flux silently destroyed a protection the user set. Measured as a non-root user, `cp a b` with `b` read-only fails "Permission denied" and only `cp -f` replaces it - flux was `cp -f` on one platform and `cp` on the other. The peer argued for reversing the fix's direction (refuse everywhere rather than make Windows overwrite) and the `cp` measurement settled it; the owner chose that direction. Fold `36a7919`, with the design consult that rule 24 requires run first. Its regression test CANNOT go red on Windows, which already refused, so it was driven red on Linux under WSL: without the guard 6 passed / 1 failed with `unwrap_err()` on an `Ok` value, with it 7 passed. **Gate at `36a7919`:** Windows `just check` exit 0, 100 tests; Linux (first run of this code on Linux at all) whole workspace green - flux-core 26, flux-fs 9, flux-platform 7 + 9 probes, flux-cli 2, lock-model 45 - and `clippy --workspace --all-targets -- -D warnings` exit 0 on BOTH platforms. Brief `.clavity/seams/fluxfs-copy-capstone.md`; design consult `.clavity/seams/readonly-publish-design.md`; agy cascade `fc856cac-6987-4a82-8f9d-f23bbbc78026`. | **Rejected by measurement:** the peer's `evidence: measured` on two rows was obtained BY the breach, not by reading. **Below the floor, both confirmed real:** the read loop does not retry `ErrorKind::Interrupted` where `std::io::copy` does (Unix-only, regular-file reads are rarely interruptible); `symlink_metadata("NUL")` on Windows fails `Uncategorized "Incorrect function."` so a device source is refused as `IO_ERROR` rather than the mandated `SPECIAL_FILE_UNSUPPORTED` - refused either way, only the code is less specific. **Design rejected with a concrete failure mode:** delete-then-rename, because a Windows target held open with `FILE_SHARE_DELETE` enters pending-delete, the rename then fails, and the destination is lost when the reader closes it. **Process:** the peer breached review-only in TWO separate disciplines this session (an adversarial-panel round wrote `PLAN_DUMP.txt` and compiled `test_file.exe`); captured in `.clavity/local-anomalies.md`. Both of its replies were flagged `[13b] TRUNCATED REPLY` for closing with the wrong discipline's token. |
| 2026-09-22 | `6472a57..8a3986a` (model-CI latency: changes A/B/D/E - one TLA+ run moved to the nightly tier, `breaklock-remote (posix)` split on the `job` key, a timeout raised, and the nightly tier taught to report failures; branch `spec/model-ci-latency`, PR #34) | 6 | GREEN, owner-confirmed at `8a3986a`; round 6 produced no findings | **Folds, each measured before committing:** `1de798a` - `notify` opened or commented on a failure issue but had NO recovery path (`grep -nE "success\(\)|issue close|issue edit"` on the file returned nothing), so the alarm never cleared and would decay from "main is broken now" to "main broke once"; a symmetric `resolve` job closes it, guarded by `needs.tier.result == 'success'` rather than `success()` alone because `success()` also holds when `tier` was SKIPPED on an empty matrix. `17449bf` - neither `notify` nor `resolve` had a BRANCH guard while `workflow_dispatch` accepts any ref; **this had already fired**: issue #33, titled "Nightly model tier failed", was opened by run `35743039548` on branch `spec/model-ci-latency`, so the spec's own procedure for verifying change E was the defect being exercised. Both jobs now require `github.ref == 'refs/heads/main'`, and the spec records that the branch-dispatch verification procedure no longer works. `8a3986a` - a comment warning that a future `breaklock` run added to the extended tier defaults to `job = "posix"` and would share the 59-minute liveness run's matrix entry. **Structural oracle:** `expected.toml` yields 11 CI jobs (was 10), `posix-fixed` holds exactly `breaklock-remote-posix-fixed-check` and `...-fixed-witness-NoLiveWriter`, `breaklock-posix-liveness` exists only in `expected-extended.toml` at `timeout_minutes = 120`. **CI:** Model `35744606862` (`36a3f0e`) and `35744782885` (`8ef66b2`) both fully green including `Suite-wide coverage union`. Brief `.clavity/seams/model-ci-capstone.md`; transcript: agy cascade `fc856cac-6987-4a82-8f9d-f23bbbc78026`. Gate at HEAD: `just check` green; `actionlint .github/workflows/model-extended.yml` exit 0. | **Rejected with evidence:** "`notify` lacks an `event_name` filter" - `model-extended.yml:96` reads `if: failure() && github.event_name != 'pull_request'`; the peer had FABRICATED both this finding and its mandated echo line, describing a file that does not exist, and the echo check is what caught it (one re-ask fixed it). "2-crash SAFETY verification left the PR tier because `breaklock-posix-check` bounds `MaxCrashes = 1`" - measured `MaxCrashes = 2`, `Breakers = ['b1','b2']`, `MaxObjs = 3`, identical to the moved run; every remaining breaklock posix run is at 2. Confirmed at the config level too: `breaklock-posix-check.cfg` and `breaklock-posix-liveness.cfg` differ ONLY in the header comment, `SYMMETRY BreakerPerms` (check only, sound for safety and unsound for liveness) and `PROPERTY UncertainLockEventuallyCleared` (liveness only) - the INVARIANT list is identical - and `run.py:1138` builds ONE TLC command line for every run with no `-simulate` or depth bound, so `kind` cannot change the search mode. What left the PR tier is the liveness PROPERTY and nothing else. The moved run has no `symmetry` key before or after, so none was lost. `model.yml` hardcodes no topology: its matrix is entirely `steps.plan.outputs.matrix`. **Below the floor:** the `--limit 50` issue lookup (needs 50+ simultaneously open issues; the repository has zero); `failure()` staying silent on a CANCELLED run (the concurrency block at `model-extended.yml:22-24` means a cancel is preemption by a superseding run, and a manual cancel does not change whether `main` is broken, so the alarm state correctly persists). **Accepted by the owner, logged in `.clavity/agy-marks/skipped.log`:** `resolve` closes the issue on a STALE verdict if an old green run on `main` is re-run, or if `main` moved past the commit the nightly verified; the sha-vs-tip guard was rejected because it reintroduces a never-clears mode every time `main` moves during a six-hour run. **Process:** the design consult for the `resolve` fix and the review rounds shared cascade `fc856cac`, so the stamp recorded SHARED-CONTEXT and round 4 reviewed a design the peer had itself endorsed; disclosed to the owner at the GREEN adjudication. Round 4 was clean but had read only ONE of the six files in the diff, so round 5 was re-pointed at the TOML/coverage side and immediately produced the `MaxCrashes` finding. |
| 2026-09-23 | `479b1177..4856742` (flux-fs single-file copy, RE-ENTRY after a breach waiver: the read-only guard's second site, the Unix access check, Windows ACL parity, the error taxonomy, and the engine `copy.rs`/`FaultFs`/CLI the first attempt never cleanly reviewed; branch `spec/flux-fs-copy`, PR #32) | 6 (rounds 3-8; rounds 1-2 ended in the BREACH WAIVER recorded in the row above, so no completion marker existed and the gate re-armed) | GREEN, owner-confirmed at `4856742`. The owner extended past the round-6 cap TWICE - once to review the engine the earlier rounds never opened, once to review that round's fold. | **Folds, each measured before committing:** `f741198` - `rename_no_replace` guarded with `Path::exists()`, which FOLLOWS symlinks, so a DANGLING symlink was replaced by the one method whose contract forbids it - the identical root cause `8cff82d` had just fixed in its sibling. `9114520` - `Permissions::readonly()` is the wrong signal on Unix: MEASURED with a root-owned `0644` file, `readonly()` reported writable, the user could not write it, `cp` REFUSED and `rename` DESTROYED it; now `rustix::fs::accessat(CWD, to, WRITE_OK, EACCESS)`. `a3a1a4f` - Windows still destroyed ACL-protected files while Unix refused, MEASURED with `icacls /deny (W,D,DC)`: attribute unset, `rename_replace` returned `Ok(())`, contents gone; now the attribute OR a `DELETE`-access probe, because neither alone suffices (read-only gives attribute=true/probe=None, ACL gives attribute=false/probe=5). `43d7822` - the TOCTOU and the sharing-violation exclusion, the latter measured at code 32 with a control that signalled only once the lock holder's handle was open. `a37c083` - `discard` destroyed the error taxonomy: wrapping `source` in `Error::other(format!(..))` turned `classify` from `DiskFull` into `IoError` on Linux, the same lesson `copy_code`'s own comment records, applied at one site and missed at this one; fixed with a new `flux_core::CopyError { cause, leftover }` after the design consult REJECTED my first attempt (a `temp_left` field on the shared `FsError`) as a boundary violation. `4856742` - removing `impl From<FsError> for CopyError` so a leaking `?` after the temporary exists FAILS TO COMPILE, proven by injection (error E0277). **Measurement:** `cargo mutants` over `copy.rs` went 2 missed of 24 to 0 missed of 29. `just check` green; 17 suites green on Windows AND Linux; 30 flux-core tests. Briefs `.clavity/seams/fluxfs-copy-capstone-r3.md`, `-r7.md`, `-r8.md`; transcript: agy cascade `fc856cac-6987-4a82-8f9d-f23bbbc78026`. | **FOUR peer claims refuted by measurement, each stated with full confidence:** (1) `access(W_OK)` returns ETXTBSY for a running binary and would break atomic binary replacement - FALSE; with a control proving the process live (`/proc/PID/exe`), `access(W_OK)` returned OK and only `open(O_WRONLY)` gave ETXTBSY. (2) Windows `MoveFileExW` backstops the rename so the asymmetry is harmless - FALSE; Rust does not call it (this file's module doc records FS-6/FS-7) and the file was destroyed. Refuting this is what UNCOVERED the ACL defect, so a round reporting zero findings held a real one in its reasoning. (3) `.access_mode(DELETE)` alone still catches the read-only attribute - FALSE (None, not 5); that rewrite would have regressed the original fix. (4) `impl From` "gave the compiler permission" to leak - premise FALSE, since before the split both sides were `FsError` and `?` compiled by identity; the conclusion was right and was folded. **Peer reversal, recorded:** at round 8 it called its own pull-up recommendation not worth the blast radius, which its own round-8 finding contradicts - a field on `FsError` could not have bought the compile-time guard. **Below the floor:** the `--limit 50` issue lookup (zero open issues); `failure()` silent on cancellation (a cancel is preemption by a superseding run). **Deferred to TODO.md, measured and named:** the atomic `renameat_with(NOREPLACE)` race in `rename_no_replace`; a blocking pre-existing temporary reported as `leftover: None`; the uncloseable read-only TOCTOU. **Process - FOUR probes failed their own controls before being caught:** `/bin/sleep` (a coreutils multi-call binary that exits instantly under another name, so the "running binary" never ran); a PowerShell exclusive lock that never took; a one-shot `fail("remove_file")` eaten by step 1's sweep, which made a new test PASS VACUOUSLY; and `cargo-mutants` itself reporting 12 of 26 MISSED in `flux-platform` when applying its headline mutant by hand fails TWO tests (0 lib tests there; the 7 real ones live in `tests/`, which the harness never ran). A surviving mutant also exposed that the read-only test could not tell OUR refusal from the OS's, since Windows `rename` refuses read-only targets unaided; it now asserts the error carries no raw OS code. |
| 2026-09-23 | `a35fb8b..c916a95` (walker PR 1 of 3, object identity: `ObjectId`, three-state `FileIdentity`, `Metadata.identity`, the Unix and Windows adapters, `FaultFs::set_identity`; branch `feat/directory-walker`, UNPUSHED). Rounds re-extended over each fold: r1 `a35fb8b..6eb0deb`, r2 `6eb0deb..172eeb5`, r3 `172eeb5..103184e`, r4 `103184e..b7f96d5`. | 4, all on the live agy peer | **ROUND-CAP WAIVER by the owner at `c916a95`**, after a GREEN at `aba3c06` that the capstone-invalidation rule then superseded. Six rounds. GREEN was adjudicated at `aba3c06`; `f2cc097` (the test-audit fold) then changed executable source, so rounds 5 and 6 ran over the new code. Round 6's single finding was folded at `c916a95` and the owner ruled \"fix it and ship without another round\" - so **`c916a95` itself is UNREVIEWED**, which is what makes this a waiver rather than a GREEN. Recorded honestly throughout: the driver, not the owner, dispositioned rounds 2-4, and the row read UNADJUDICATED until the owner first ruled. | Rounds 5-6 folds: `74be368` (the fake mints identity at creation instead of hashing the path - round 5 found that carrying a PATH-derived identity across a rename freed the old hash, so a file recreated at the source collided with the renamed object; MEASURED collide=true. A path hash cannot be made correct: identity must be invariant per object AND distinct across objects, and a hash gives one or the other. Also folded: a rename now REPLACES the destination's times and perms rather than merging, and `remove_file` clears the path's state so a later file does not inherit a dead object's identity - both MEASURED as stale `Some(UnixMode(0o600))` beforehand), `c916a95` (round 6: `rename_replace` never checked the source existed, so `move_object` conjured an empty destination, returned Ok and minted no identity - MEASURED, the next `metadata` PANICKED. Folded rather than deferred because this work's own panic caused the crash). ROADMAP section 24 consult ran before the round-5 fold and CHANGED it: the driver proposed reporting `Unavailable` for an unhooked creation path, the peer refused because `Unavailable` is a state the walker handles gracefully so it would degrade silently, and it named `add_special` as an unhooked path the driver had missed - verified, it does insert into `files`. Earlier folds: `c4d1ea3` (the only Windows coverage of `FILE_FLAG_OPEN_REPARSE_POINT` was a test that SKIPPED without Developer Mode and still reported ok - MEASURED, removing the flag left the suite 12 passed 0 failed; replaced with a junction fallback, which needs no privilege, and verified in both directions), `172eeb5` (the plan's claim that identity on `Metadata` preserves atomicity holds on Unix ONLY - the Windows adapter already calls `symlink_metadata` then opens the path again, so merging those two opens is a CORRECTNESS fix for PR 2, not an optimisation), `b7f96d5` (the setup result was a bool, so the assert guessed a Windows cause even on Unix; now `Result<(), String>` carrying cmd's own stderr), `aba3c06` (recorded the write-protection guard's swallowed errors). Gate at HEAD: `just check` exit 0; Windows `flux-fs` 10, `flux-core` 31, `tests/std_fs.rs` 12, `tests/fs_semantics.rs` 9; Linux `tests/std_fs.rs` 15, `flux-core` 31, `flux-fs` 10. Briefs `.clavity/seams/pr1-capstone.md` and `-r2` `-r3` `-r4`. | REJECTED by measurement: `FILE_ID_128` was claimed not to implement `Default` (it does, windows-sys 0.61.2 `mod.rs:1542`, and the code compiles); `m.modified().ok()` as information loss (the field is declared `Option<SystemTime>`, so `None` IS the contract). ALREADY-KNOWN and out-of-range, both PR #32 code with 0 occurrences in this diff: `rename_no_replace` check-then-act (`TODO.md:106-113` records it verbatim including the same fix), and `destination_is_write_protected`'s `Err(_) => false` (recorded in `TODO.md` by `aba3c06`). DEFERRED to the walker: transient-failure vs unsupported-filesystem both reporting `Unavailable`. NOT ADOPTED, recorded as a judgement: the peer argued the junction fallback is too clever after three rounds of defects and would use a native junction API or rely on CI with Developer Mode - fair, but the cure is a new dependency (§67 governs) or more unsafe FFI in a test. **Process, on the record:** the peer breached review-only during the plan panel, writing five `.rs` files to `scratch/` at the repository ROOT and COMPILING them to `.exe`; reverted, and naming it in the next brief stopped it recurring for four rounds. The driver ALSO missed its own envelope check that round, which is how the breach went unnoticed until later. |
| 2026-09-24 | `ec58829..5263ca6` (walker PR 2 of 3, the directory walk: `FileType`, `DirEntry`, `read_dir`, `create_dir`, `Code::DirectoryChangedDuringScan`, the `FaultFs` `PathBuf` rekeying, and `Walk` with its ordering, non-terminal errors, depth cap, cycle detection and type-change detection; branch `feat/walker-walk`, UNPUSHED). Range re-extended over each fold: r1 `ec58829..d3a849e`, r2 `ec58829..a6f69d6`, r3 `ec58829..5263ca6`. The plan artifact was EXCLUDED from every round by pathspec - 2,536 lines of markdown already through five adversarial panel rounds, and including it would have buried the executable findings. | 3, all on the live agy peer | **GREEN, owner-confirmed at `5263ca6`.** Round 3 returned an empty findings array with all four derived seats clean. Credited rather than rubber-stamped: it stated specifics the brief never supplied, and one was MEASURED - `walk_with_depth(.., 0)` yields `files=["top.txt"] errs=["sub"]`, exactly as claimed. Both below-floor items were read as carefully as findings. | **Folds, each measured before committing.** `a6f69d6` (r1, 2 findings): `PathBuf::join` DISCARDS its base on an absolute component - MEASURED, `join("sub", "/etc/shadow")` is `"/etc/shadow"` - so an adapter returning an absolute `DirEntry::name` escaped the walk root; now documented on the trait AND enforced in `Walk::next`. Plus `FaultFs::create_dir` treating only `/` as a root, which rejected a valid Windows drive-root create. `5263ca6` (r2, 3 of 4 findings): **the r1 fix was itself defective** - the panic fired AFTER `entries.next()` consumed the bad entry and nothing poisoned the iterator, so a consumer catching the unwind and resuming was handed the next sibling and the bad entry vanished. MEASURED before the fix: `PROBE second=YIELDED File { path: "good.txt" }`. Fixed by `self.stack.clear()` BEFORE the panic; mutant-proven (removing the clear reproduces that exact yield). Plus `create_dir` accepting a RELATIVE path (parent `""`, whose `parent()` is `None`, so the check was skipped) - guarded with `has_root()`, NOT `is_absolute()`, because `Path::new("/r").is_absolute()` is FALSE on Windows and would reject every path the tests use. **REJECTED by measurement:** that `as_encoded_bytes()` sorting diverges from NTFS's UTF-16 collation above the BMP - that divergence IS the design, pinned deliberately by `encoded_bytes_order_is_the_ordering_contract_not_utf16` at `crates/flux-fs/src/fs.rs:303`. **DISCARDED-BELOW-FLOOR:** returning `None` rather than re-panicking after the poison - re-panicking from `next()` during a consumer's unwind would be panic-in-panic and abort the process, so `None` is the correct choice; and `max_depth=0` refusing every subdirectory, which is what a cap of 0 means. **ROUND-1 REVIEW-ONLY BREACH, surfaced and NOT waived:** the peer edited `Cargo.toml` and `Cargo.lock` to add a workspace member and wrote a 78KB `diff.txt` to the repo root; all three reverted, HEAD never moved, and the owner chose fold-and-re-run, so this GREEN rests on a clean round rather than a breach waiver. **§24 stamp: SHARED-CONTEXT** - the mandatory design consult for r1's new code and the review ran in one cascade; surfaced at adjudication. Gates: Windows `153 tests run: 153 passed, 1 skipped`, Linux 155 passed. |
| 2026-09-25 | `cfeb09b..9f9c073` (walker cut 3, atomic no-replace publication; branch `feat/copy-tree`) | 7 (6 + 1 owner-authorized round past the cap on `161bebc..add3110`) | GREEN, owner-confirmed | Folds: `c751911` (interior-NUL rejection + the missing-source-before-occupied-target ordering test), `7c23b0d` (the negotiated same-object veto), `f29c6c6` (lexical half, for the unavailable-identity path), `37f12e0` (nest both halves under the probe of `to`), `161bebc` (pin that the probe must ask for NO access), `add3110` and `9f9c073` (two comment claims later commits had falsified). Transcripts: agy cascades `1c78560e-263c-4a5c-9cc5-f80dc1003ff9` (rounds 1-2) and `dd775fb9-daf1-49a1-a897-9092dd30a9bd` (rounds 3-7). Briefs under `.clavity/seams/cut3-capstone*.md`. Gates at `9f9c073`: Windows `170 tests run: 170 passed, 1 skipped`, Linux 170 passed, on stable 1.98.1. | **TWO REGRESSIONS THIS CUT INTRODUCED, both MEASURED and folded:** going direct to `MoveFileExW` lost `std`'s interior-NUL rejection, so a publish at `to\0bar` landed at `to` and returned Ok; and the Windows arm stopped refusing a same-object target, succeeding on `from == to` and CONSUMING the source name for two hardlinks. **FOLD-SPAWNS-A-DEFECT held every round:** rounds 3, 4 and 5 each found a defect in the previous round's fix, including an error-priority inversion the driver introduced while fixing round 3. **REJECTED by measurement:** `faultfs-error-priority-inversion` (peer retracted it, citing `may_delete_dentry` returning `-ENOENT` before the filesystem evaluates the flag); `case-only-rename-vetoed` as `blocking` (the check-then-act body it replaced refused it identically, and macOS refuses it too - now pinned by a test so it is not "fixed" later); `lexical-guard-bypassed-by-probe-failure` (premise true, consequence false - an exclusive lock still PROBES because `access_mode(0)` conflicts with nothing, and a deny ACE fails `MoveFileExW` too, raw 5). **A SIMPLIFICATION THE DRIVER PROPOSED AND THE PEER CORRECTLY KILLED:** a bare `if probe(to).is_ok() { refuse }` would answer AlreadyExists for a missing source over an occupied target. **PEER FIX REJECTED while its finding was accepted:** dropping the lexical guard would have re-opened the `from == to` hole the same peer raised one round earlier. **DEFERRED-TO-ANOMALIES:** `crates/flux-platform/src/std_fs.rs:431` * 2026-09-25 * unverified - on a mount lacking `FILE_ID_INFO` but supporting second names for one object (SMB, or a subst drive/junction over one), the veto is bypassed; not measurable here, no SMB or FAT32 volume mounted. **DISCARDED-BELOW-FLOOR:** a contrived deny-ACE decorrelation the peer itself judged not worth acting on; and `NtSetInformationFile` + `FileRenameInformation` (`ReplaceIfExists = FALSE`) as a future-cut replacement that would delete the veto entirely. **METHOD ERROR CORRECTED MID-REVIEW:** `cargo nextest` fail-fasts by default, so several mutant runs were truncated (`5/8`, `5/9`, `6/9 tests run`) and their "nothing else went red" was unproven; every mutant was re-measured with `--no-fail-fast`. **ADJUDICATION NUANCE:** round 7's clean round was at `add3110`; HEAD `9f9c073` is one comment-only commit past it, containing nothing but the peer's own below-floor analysis applied, and the owner confirmed GREEN with that stated. No review-only breach in any round. |
| 2026-09-25 | `f070f97..209dc19` (149.7 handle-relative destination writes, 26 commits; branch `feat/handle-relative-writes`) | 12 (owner waived the round-6 cap with "rounds until green"; round 12 returned an empty findings array) | GREEN at round 12, owner-confirmed at `209dc19` | 28 findings folded across 26 commits. The four that mattered most: `71f3d37` a u16 UNICODE_STRING length wrap that created a 100-character file when asked for a 32868-character name and returned Ok, and at exactly 32768 wrapped to zero, which NtCreateFile reads as the parent directory itself; `71f3d37` remove_file deleting ordinary directories on Windows where POSIX refused them; `b20359d` and `a2454a8` FAT32 broken twice, first all deletion then all directory traversal, both measured on a real volume; `b624314` an unpaired surrogate smuggling any name past every check because one branch of the validator validated nothing. Transcript: agy cascade `dd775fb9-daf1-49a1-a897-9092dd30a9bd`. Gates at `209dc19`: Windows `just check` 207 passed / 2 skipped; WSL workspace 196 passed / 2 skipped. | REFUTED by measurement, four: POSIX classify seeing ELOOP rather than ENOTDIR (two green tests reach the SafetyRejected branch, the only path producing that code); a directory-held name returning STATUS_ACCESS_DENIED (measured 0xC00000BA); the dot and dot-dot asymmetry being reachable; Windows type_of misclassifying a junction (both metadata paths measured Symlink). REFUSED WITH REASON, one: a stat-then-rename fallback for filesystems lacking RENAME_NOREPLACE, which would reintroduce the check-then-act window this cut closes (measured previously at 400 of 400 correct for the atomic form against 3188 wrong outcomes for check-then-act); failing loudly is correct and the up-front probe belongs to the engine. DEFERRED as tracked debt: the FaultFs node-id re-key (owner-agreed to cut 4, where its caller defines the requirements) and the 241.5 up-front no-replace probe, for which this review produced the measured evidence that such destinations are real. NOTE: the peer breached the review-only envelope in at least five rounds, writing probe sources and built executables into the repository root and twice appending a dependencies section to the root Cargo.toml; all reverted, HEAD unaffected. |
| 2026-09-25 | `209dc19..3f8c79c` (149.7 handle-relative destination writes, RE-RUN over the test-audit delta only; branch `feat/handle-relative-writes`) | 2 | GREEN at round 2, empty findings array | **Why it re-ran:** the AGY-TEST-AUDIT that followed the round-12 GREEN found a coverage gap whose closure required a SOURCE change, and the capstone-invalidation rule makes that invalidate the prior GREEN. The delta is four commits: `e719548` (tests only), `4c4b8c5` (separating the reparse judgement from the syscalls that feed it), the restoration of the NTSTATUS to the fallback's refusal message, and `08dede4` (round 1's two folds). Round 1 found both of its findings at the SEAM the refactor created -- the construction of the variant -- and both were folded: `Tagged` was being built without checking the reparse bit, feeding a tag the NT ABI says to ignore into judges that also did not check it; and `Untagged` with the directory attribute, the exact FAT32 case the fallback exists for, was the one row of the decision table with no unit test. Round 2 returned GREEN and noted one real inaccuracy in a test comment, corrected at `3f8c79c`. Gates at `3f8c79c`: Windows `just check` 216 passed / 2 skipped; WSL workspace 196 passed / 2 skipped; the two FAT32 tests pass against a real volume. | **MECHANISM REJECTED, FINDING FOLDED -- worth recording as a pattern.** Round 1 argued the reparse-tag field could hold uninitialised kernel pool memory which might match the surrogate mask and allow a plain directory to be deleted. MEASURED FALSE: both wrappers zero-initialise the struct before the call, so an unwritten tag reads 0, `is_name_surrogate(0)` is false, and the directory is refused. The claim that it was exploitable today does not hold. The STRUCTURAL half of the same finding earned the fold on its own merits -- consulting a field the ABI says to ignore is relying on unspecified behaviour whatever the current kernel happens to write -- so the fix is justified by a mechanism that was verified rather than by the one that was asserted. Behaviour preservation was checked independently by the driver across all eight combinations of both judges before the round was sent, and the peer agreed the table is isomorphic to the control flow it replaced. |
| 2026-09-25 | `aa05fe4..301012d` (PR #44 cross-platform stat fix: macOS `st_mode: u16`/`st_dev: i32` and Linux clippy `unnecessary_cast`; branch `feat/handle-relative-writes`) | 3 | GREEN at round 3, owner-confirmed at `301012d`, empty findings array | Fix `b0c63a5`. Folds: `54c2889` (the handle-vs-path metadata test; before it, forcing the handle side's `dev` to 0 left all 196 Linux tests green, after it the `dev=0` and `mode=0` mutants each turn it red) and `301012d` (a separate `Clippy (macOS)` CI job; not a matrix, because "Clippy" is a required check and a rename would block every PR). Transcript: agy cascade `dab4b353-0c93-4e13-b2e1-cca9ee269d73`; briefs and replies `.clavity/seams/pr44-xplat-capstone*.md`. Gates at `301012d`: Windows `just check` 216 passed / 2 skipped; WSL workspace 197 passed / 2 skipped; `aarch64-apple-darwin` clippy clean (workspace minus the root crate, whose criterion dev-dependency needs a macOS C toolchain); actionlint clean. | **REFUTED by measurement:** the peer's round-1 alternative `u32::from(st.st_mode)` as "strictly better" - clippy `useless_conversion` rejects `u32::from` on a `u32`, so it would re-break Linux clippy. **DRIVER RULING OVERTURNED:** the driver called the excluded root crate a "local blind spot only"; CI ran clippy on Linux alone, so it was a global one - folded as `301012d`. **REVIEW-ONLY BREACH, round 1:** the peer added its scratch crate to the root `Cargo.toml` workspace members; reverted, HEAD unaffected, owner chose a clean re-run. Naming the breach and the legal path (`--manifest-path`) in the round-2 brief ended it: rounds 2 and 3 wrote only inside the sanctioned scratch directory. **OPEN, owner's setting:** `Clippy (macOS)` is not a required check, and auto-merge waits only on required checks. |
